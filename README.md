 # sid_sale_line_views_ov

## Proposito
Overrides de vistas (OV) para `sale.order.line` (tree/search/form) con campos custom.

## Dependencias
- `sid_sale_line_custom_fields` (meta), que instala `sid_sale_line_core` y `sid_sale_line_stock`.

## Contenido
Ver carpeta `views/`.
